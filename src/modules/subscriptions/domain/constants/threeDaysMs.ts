/**
 * используется для подписки
 * если текущая подпивска кончается через 3 дня и меньше
 * то подписка ставновится доступна для покупки
 */
export const THREE_DAYS_MS = 3 * 24 * 60 * 60 * 1000; // 3 days in milliseconds
