Дропы и продукты
drops

Событие (релиз).
```
drops (
  id            uuid pk,
  name          text,
  description   text,
  starts_at     timestamptz,
  ends_at       timestamptz,
  is_active     boolean
)
```